jdk:
  - oraclejdk8

install:
   - ./gradlew -p kernel/base publishToMavenLocal
   - ./gradlew -p kernel/clojure publishToMavenLocal
   - ./gradlew -p kernel/groovy publishToMavenLocal
   - ./gradlew -p kernel/java publishToMavenLocal
   - ./gradlew -p kernel/scala publishToMavenLocal

