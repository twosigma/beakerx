version 'unspecified'

apply plugin: 'java'

sourceCompatibility = 1.8


ext.kernelIdName = 'demoProjects'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

staticContent {
    ext.executeStaticContent = false
}