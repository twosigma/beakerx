{
  "name": "beaker-tests",
  "scripts": {
    "start": "pioneer --require util --require steps --require widgets -f pretty --driver=firefox --prevent-browser-reload"
  },
  "dependencies": {
    "pioneer": "0.6.3"
  }
}
